import{r,opts}from'./utils.js';function q(text,options,answer,thai=''){return{text,options:opts(options,answer),answer,thai}}export function getBank(slug,id,topic,d='medium'){let b=[];for(let i=0;i<50;i++){let a=r(1,20),c=r(1,20),ans=a+c;b.push(q(`${topic}: ${a} + ${c} = ?`,[ans,ans+1,ans-1,ans+2],ans,'คำนวณหาคำตอบ'))}return b}
