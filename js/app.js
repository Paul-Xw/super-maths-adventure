import{CONFIG,WORLD_LIST,WEAPONS,PETS}from'./config.js';
import{ok,saveScore,leaderboard,ensureStudent,studentProfile,classDashboard,upsertQuestion,fetchQuestions}from'./supabase-client.js';
import{sample,esc,unesc,same,calcStars,difficultyFromAccuracy,downloadText}from'./utils.js';
import{getBank,getLocalQuestions,addLocalQuestion,deleteLocalQuestion,setLocalQuestions}from'./questionBank.js';
import{runQAQuestions}from'./qaEngine.js';
import{unlockAudio,toggleAudio,isAudioOn,sfx,playBgm}from'./audio.js';

const app=document.getElementById('app');
document.addEventListener('click',e=>{if(e.target&&e.target.id==='soundBtn'){unlockAudio();const on=toggleAudio();e.target.textContent=on?'🔊 Sound ON':'🔇 Sound OFF';if(on)playBgm(currentWorld.world||1)}});

let currentWorld=WORLD_LIST[0],scenes=[];
let p=JSON.parse(localStorage.getItem(CONFIG.storageKey)||'{}');
let state={player:p.player||CONFIG.defaultPlayer,classCode:p.classCode||CONFIG.defaultClass,role:p.role||'student',idx:0,q:0,qs:[],score:0,correct:0,lives:3,streak:0,bossHp:100,playerHp:100,difficulty:'medium',phase:'start',inventory:p.inventory||{weapon:'laser',pet:'owl',ownedWeapons:['laser'],ownedPets:['owl']},progress:p.progress||{unlockedWorld:1,unlocked:0,stars:Array(20).fill(0),xp:0,coins:0,review:[]}};
const save=()=>localStorage.setItem(CONFIG.storageKey,JSON.stringify(state));
const badge=(t,c='bg-cyan-100 text-cyan-800')=>`<span class="pill ${c}">${t}</span>`;
const weapon=()=>WEAPONS.find(x=>x.id===state.inventory.weapon)||WEAPONS[0];
const pet=()=>PETS.find(x=>x.id===state.inventory.pet)||PETS[0];
const online=()=>ok?badge('Supabase Connected','bg-green-100 text-green-800'):badge('Local Mode','bg-amber-100 text-amber-800');
const shell=c=>app.innerHTML=`<button id="soundBtn" class="sound-toggle">${isAudioOn()?'🔊 Sound ON':'🔇 Sound OFF'}</button><header class="text-center text-white mb-6"><div class="inline-flex px-4 py-2 rounded-full bg-white/10 mb-3 font-bold">🌍 Super Maths Adventure v5</div><h1 class="text-5xl md:text-7xl font-black">Super Maths Adventure</h1><p class="text-cyan-100">Teacher CMS • Question Bank • Game RPG • QA</p></header>${c}`;
function popText(txt,x=innerWidth/2,y=innerHeight/2){const e=document.createElement('div');e.className='fx-pop';e.textContent=txt;e.style.left=x+'px';e.style.top=y+'px';document.body.appendChild(e);setTimeout(()=>e.remove(),900)}
function toast(txt){const e=document.createElement('div');e.className='achievement-toast';e.textContent=txt;document.body.appendChild(e);setTimeout(()=>e.remove(),3000)}
function saveInput(){state.player=document.getElementById('playerName')?.value.trim()||state.player;state.classCode=(document.getElementById('classCode')?.value||state.classCode).trim().toUpperCase();state.role=document.getElementById('role')?.value||state.role;save()}
async function enter(){unlockAudio();sfx('click');saveInput();await ensureStudent(state.player,state.classCode);state.role==='teacher'?renderTeacherHome():renderWorldMap()}
function renderStart(){shell(`<main class="glass rounded-[2rem] p-6 md:p-10 max-w-6xl mx-auto"><div class="grid md:grid-cols-2 gap-8 items-center"><section><div class="text-8xl mb-3">🗺️🧑‍🏫</div><h2 class="text-4xl font-black">Super Maths Adventure v5</h2><div class="mt-3">${online()} ${badge('Worlds: '+WORLD_LIST.length,'bg-purple-100 text-purple-800')} ${badge('CMS Ready','bg-green-100 text-green-800')}</div><p class="text-slate-600 mt-4 text-lg">Teacher CMS + Question Bank: ครูเพิ่มโจทย์เองได้ ไม่ต้องแก้โค้ด</p><div class="grid md:grid-cols-3 gap-3 mt-5"><input id="playerName" class="px-4 py-3 rounded-2xl border font-bold" value="${state.player}"><input id="classCode" class="px-4 py-3 rounded-2xl border font-bold" value="${state.classCode}"><select id="role" class="px-4 py-3 rounded-2xl border font-bold"><option value="student" ${state.role==='student'?'selected':''}>Student</option><option value="teacher" ${state.role==='teacher'?'selected':''}>Teacher</option></select></div><div class="grid md:grid-cols-4 gap-3 mt-6"><button id="joinBtn" class="btn px-7 py-4 rounded-2xl bg-cyan-500 text-white font-black">Enter</button><button id="shopBtn" class="btn px-7 py-4 rounded-2xl bg-green-400 font-black">Shop</button><button id="dashBtn" class="btn px-7 py-4 rounded-2xl bg-slate-900 text-white font-black">Dashboard</button>${state.role==='teacher'?'<button id="cmsBtn" class="btn px-7 py-4 rounded-2xl bg-amber-400 font-black">Teacher CMS</button>':''}</div></section><section class="dark rounded-[2rem] p-6"><h3 class="text-2xl font-black">Loadout</h3><div class="grid gap-3 mt-4"><div class="p-4 rounded-xl bg-white/10">Weapon ${weapon().icon} ${weapon().name}</div><div class="p-4 rounded-xl bg-white/10">Pet <span class="pet">${pet().icon}</span> ${pet().name}</div><div class="p-4 rounded-xl bg-white/10">Coins 🪙 ${state.progress.coins||0}</div><div class="p-4 rounded-xl bg-white/10">Local Questions: ${getLocalQuestions().length}</div></div></section></div></main>`);document.getElementById('joinBtn').onclick=enter;document.getElementById('shopBtn').onclick=renderShop;document.getElementById('dashBtn').onclick=()=>{saveInput();state.role==='teacher'?renderTeacherHome():renderStudentDashboard()};if(document.getElementById('cmsBtn'))document.getElementById('cmsBtn').onclick=()=>{saveInput();renderTeacherCMS()}}
function renderTeacherHome(){shell(`<main class="glass rounded-[2rem] p-6 max-w-6xl mx-auto"><h2 class="text-4xl font-black">🧑‍🏫 Teacher Dashboard</h2><p class="text-slate-600">${state.player} • ${state.classCode}</p><div class="grid md:grid-cols-4 gap-4 mt-6"><button id="cms1" class="card btn text-left"><div class="text-5xl">📝</div><h3 class="text-xl font-black">Question Bank</h3><p>Add/Edit questions</p></button><button id="cms2" class="card btn text-left"><div class="text-5xl">📥</div><h3 class="text-xl font-black">Import CSV</h3><p>Bulk upload</p></button><button id="cms3" class="card btn text-left"><div class="text-5xl">🧪</div><h3 class="text-xl font-black">Run QA</h3><p>Check questions</p></button><button id="cms4" class="card btn text-left"><div class="text-5xl">🌍</div><h3 class="text-xl font-black">Preview Game</h3><p>Student view</p></button></div><button id="homeBack" class="btn mt-6 px-6 py-3 rounded-2xl bg-cyan-500 text-white font-black">Home</button></main>`);document.getElementById('cms1').onclick=renderTeacherCMS;document.getElementById('cms2').onclick=renderImportCSV;document.getElementById('cms3').onclick=renderQA;document.getElementById('cms4').onclick=renderWorldMap;document.getElementById('homeBack').onclick=renderStart}
async function renderTeacherCMS(){const local=getLocalQuestions();let onlineRows=[];if(ok){const results=await Promise.all(WORLD_LIST.map(w=>fetchQuestions(w.slug)));onlineRows=results.flat().filter(Boolean)}const rows=[...onlineRows.map(x=>({...x,source:'online'})),...local];shell(`<main class="glass rounded-[2rem] p-6 max-w-7xl mx-auto"><div class="flex flex-col md:flex-row justify-between gap-3"><div><h2 class="text-4xl font-black">📝 Question Bank CMS</h2><p>Online: ${onlineRows.length} • Local: ${local.length}</p></div><div>${online()} ${badge('QA before Publish','bg-amber-100 text-amber-800')}</div></div><div class="grid md:grid-cols-2 gap-6 mt-6"><section class="card"><h3 class="text-2xl font-black mb-3">Add Question</h3><div class="grid gap-3"><select id="qWorld" class="px-4 py-3 rounded-2xl border font-bold">${WORLD_LIST.map(w=>`<option value="${w.slug}">${w.world}. ${w.topic}</option>`).join('')}</select><input id="qTopic" class="px-4 py-3 rounded-2xl border" placeholder="Topic / Skill เช่น Absolute Value"><textarea id="qEn" class="px-4 py-3 rounded-2xl border" rows="2" placeholder="Question English"></textarea><textarea id="qTh" class="px-4 py-3 rounded-2xl border" rows="2" placeholder="Question Thai / Hint ไม่ใส่คำตอบ"></textarea><div class="grid grid-cols-2 gap-2"><input id="cA" class="px-4 py-3 rounded-2xl border" placeholder="Choice A"><input id="cB" class="px-4 py-3 rounded-2xl border" placeholder="Choice B"><input id="cC" class="px-4 py-3 rounded-2xl border" placeholder="Choice C"><input id="cD" class="px-4 py-3 rounded-2xl border" placeholder="Choice D"></div><input id="ans" class="px-4 py-3 rounded-2xl border font-bold" placeholder="Answer ต้องตรงกับ choice"><textarea id="expEn" class="px-4 py-3 rounded-2xl border" rows="2" placeholder="Explanation English"></textarea><textarea id="expTh" class="px-4 py-3 rounded-2xl border" rows="2" placeholder="Explanation Thai"></textarea><select id="diff" class="px-4 py-3 rounded-2xl border"><option value="1">★ Easy</option><option value="2">★★</option><option value="3">★★★ Medium</option><option value="4">★★★★</option><option value="5">★★★★★ Hard</option></select><div class="grid grid-cols-2 gap-2"><button id="saveLocal" class="btn py-3 rounded-2xl bg-slate-900 text-white font-black">Save Local</button><button id="saveOnline" class="btn py-3 rounded-2xl bg-green-500 font-black">Save Online</button></div></div></section><section class="card"><h3 class="text-2xl font-black mb-3">Questions</h3><div class="flex gap-2 mb-3 flex-wrap"><button id="qaBtn" class="btn px-4 py-2 rounded-2xl bg-amber-100 text-amber-800 font-black">Run QA</button><button id="importBtn" class="btn px-4 py-2 rounded-2xl bg-blue-100 text-blue-800 font-black">Import CSV</button><button id="exportBtn" class="btn px-4 py-2 rounded-2xl bg-green-100 text-green-800 font-black">Export JSON</button><button id="clearLocalBtn" class="btn px-4 py-2 rounded-2xl bg-rose-100 text-rose-800 font-black">Clear Local</button></div><div class="max-h-[560px] overflow-auto"><table class="cms-table"><thead><tr><th>World</th><th>Question</th><th>Ans</th><th>Src</th><th></th></tr></thead><tbody>${rows.slice(0,200).map(q=>`<tr><td>${q.world_slug}</td><td>${esc(q.question_en||q.text)}</td><td><b>${esc(q.answer)}</b></td><td>${q.source||'local'}</td><td>${q.source==='local'?`<button class="delLocal text-rose-700 font-black" data-id="${q.id}">Delete</button>`:''}</td></tr>`).join('')}</tbody></table></div></section></div><button id="cmsBack" class="btn mt-6 px-6 py-3 rounded-2xl bg-cyan-500 text-white font-black">Back</button></main>`);document.getElementById('saveLocal').onclick=()=>saveQuestion(false);document.getElementById('saveOnline').onclick=()=>saveQuestion(true);document.getElementById('qaBtn').onclick=renderQA;document.getElementById('importBtn').onclick=renderImportCSV;document.getElementById('exportBtn').onclick=()=>downloadText('question_bank.json',JSON.stringify(getLocalQuestions(),null,2));document.getElementById('clearLocalBtn').onclick=()=>{if(confirm('Clear all local questions?')){setLocalQuestions([]);renderTeacherCMS()}};document.querySelectorAll('.delLocal').forEach(b=>b.onclick=()=>{deleteLocalQuestion(b.dataset.id);renderTeacherCMS()});document.getElementById('cmsBack').onclick=renderTeacherHome}
async function saveQuestion(onlineSave){const q={world_slug:document.getElementById('qWorld').value,lesson_id:'general',topic:document.getElementById('qTopic').value,difficulty:Number(document.getElementById('diff').value),question_en:document.getElementById('qEn').value,question_th:document.getElementById('qTh').value,choice_a:document.getElementById('cA').value,choice_b:document.getElementById('cB').value,choice_c:document.getElementById('cC').value,choice_d:document.getElementById('cD').value,answer:document.getElementById('ans').value,explanation_en:document.getElementById('expEn').value,explanation_th:document.getElementById('expTh').value,type:'multiple_choice',published:true,created_by:state.player};const qa=runQAQuestions([q]);if(qa.fail){alert('QA Failed: '+qa.issues.map(x=>x.message).join(', '));return}if(onlineSave&&ok){const res=await upsertQuestion(q);if(res.ok)toast('Saved Online');else{addLocalQuestion(q);toast('Online failed, saved Local')}}else{addLocalQuestion(q);toast('Saved Local')}renderTeacherCMS()}
function renderImportCSV(){shell(`<main class="glass rounded-[2rem] p-6 max-w-5xl mx-auto"><h2 class="text-4xl font-black">📥 Import CSV</h2><p class="text-slate-600">Header: world_slug,topic,question_en,question_th,choice_a,choice_b,choice_c,choice_d,answer,explanation_en,explanation_th,difficulty</p><textarea id="csvText" class="w-full mt-4 p-4 rounded-2xl border font-mono" rows="14" placeholder="Paste CSV here"></textarea><div class="flex gap-3 mt-4"><button id="loadSample" class="btn px-5 py-3 rounded-2xl bg-blue-100 text-blue-800 font-black">Sample</button><button id="importLocal" class="btn px-5 py-3 rounded-2xl bg-slate-900 text-white font-black">Import Local</button><button id="back" class="btn px-5 py-3 rounded-2xl bg-cyan-500 text-white font-black">Back</button></div></main>`);document.getElementById('loadSample').onclick=()=>document.getElementById('csvText').value=`world_slug,topic,question_en,question_th,choice_a,choice_b,choice_c,choice_d,answer,explanation_en,explanation_th,difficulty\nworld-1-integers,Addition,What is -5 + 9?,ลบห้าบวกเก้าได้เท่าไร,4,5,-4,14,4,-5 + 9 = 4,9 มากกว่า 5 จึงเหลือ 4,2`;document.getElementById('importLocal').onclick=()=>importCSV(document.getElementById('csvText').value);document.getElementById('back').onclick=renderTeacherCMS}
function parseCSV(text){const lines=text.trim().split(/\r?\n/);const header=lines.shift().split(',').map(x=>x.trim());return lines.filter(Boolean).map(line=>{const parts=line.split(',');let o={};header.forEach((h,i)=>o[h]=parts[i]||'');return o})}
function importCSV(text){try{const rows=parseCSV(text);const qa=runQAQuestions(rows);if(qa.fail){alert('QA Failed: '+qa.issues.map(x=>x.scope+' '+x.message).join('\\n'));return}rows.forEach(r=>addLocalQuestion({...r,published:true,type:'multiple_choice'}));toast('Imported '+rows.length+' questions');renderTeacherCMS()}catch(e){alert('CSV error: '+e.message)}}
async function renderQA(){const local=getLocalQuestions();let onlineRows=[];if(ok){const results=await Promise.all(WORLD_LIST.map(w=>fetchQuestions(w.slug)));onlineRows=results.flat().filter(Boolean)}const all=[...onlineRows,...local];const qa=runQAQuestions(all);shell(`<main class="glass rounded-[2rem] p-6 max-w-6xl mx-auto"><h2 class="text-4xl font-black">🧪 QA Runner</h2><div class="grid md:grid-cols-4 gap-4 mt-6"><div class="card">Questions<br><b class="text-4xl">${qa.total}</b></div><div class="card">Fails<br><b class="text-4xl">${qa.fail}</b></div><div class="card">Warnings<br><b class="text-4xl">${qa.warn}</b></div><div class="card">Status<br><b class="text-4xl">${qa.fail?'❌':'✅'}</b></div></div><div class="grid gap-2 mt-6">${qa.issues.length?qa.issues.map(i=>`<div class="p-4 rounded-2xl ${i.level==='fail'?'qa-fail':'qa-warn'}"><b>${i.level.toUpperCase()}</b> • ${i.scope} • ${i.message}</div>`).join(''):'<div class="p-4 rounded-2xl qa-pass">All questions passed QA.</div>'}</div><button id="qaBack" class="btn mt-6 px-6 py-3 rounded-2xl bg-cyan-500 text-white font-black">Back</button></main>`);document.getElementById('qaBack').onclick=state.role==='teacher'?renderTeacherHome:renderStart}
function buildScenes(w){return Array.from({length:8},(_,i)=>({id:i===7?'boss':'s'+(i+1),name:['Core Skill','Practice Zone','Mini Challenge','Mixed Problems','Review Gate','Speed Mission','Project Quest','Boss Battle'][i],icon:w.icon,enemy:w.enemy,topic:w.topic,loop:['Story','Learn','Worked Example','Easy Mission','Mini Game','Medium Mission','Boss/Review'],worked:[[w.topic+' example','Read, identify the rule, calculate, then check.'],['Review','The same skill returns in different forms.']]}))}
function renderWorldMap(){playBgm(currentWorld.world||1);shell(`<main class="glass rounded-[2rem] p-6"><div class="flex justify-between flex-wrap gap-3 mb-6"><div><h2 class="text-3xl font-black">🌍 World Map</h2><p>${state.player} • ${state.classCode}</p></div><div>${online()} ${badge('🪙 '+(state.progress.coins||0),'bg-yellow-100 text-yellow-800')}</div></div><div class="grid md:grid-cols-4 gap-5">${WORLD_LIST.map((w,i)=>`<div class="world-card card bg-gradient-to-br ${w.color} text-white"><div class="world-badge">${badge(w.strand,'bg-white/80 text-slate-900')}</div><div class="text-6xl">${w.icon}</div><div class="mt-3 text-sm font-black opacity-80">WORLD ${w.world}</div><h3 class="text-2xl font-black">${w.name}</h3><p>${w.topic}</p><div class="mt-3 path-line"></div><button class="worldBtn btn mt-5 w-full py-3 rounded-2xl bg-white text-slate-900 font-black" data-i="${i}">Enter World</button></div>`).join('')}</div><div class="mt-6 flex gap-3"><button id="homeBtn" class="btn px-5 py-3 rounded-2xl bg-slate-200 font-black">Home</button><button id="leaderBtn" class="btn px-5 py-3 rounded-2xl bg-yellow-100 text-yellow-800 font-black">Leaderboard</button></div></main>`);document.querySelectorAll('.worldBtn').forEach(b=>b.onclick=()=>loadWorld(WORLD_LIST[+b.dataset.i]));document.getElementById('homeBtn').onclick=renderStart;document.getElementById('leaderBtn').onclick=renderLeaderboard}
function loadWorld(w){currentWorld=w;playBgm(w.world||1);sfx('click');scenes=buildScenes(w);state.idx=0;state.progress.unlocked=7;renderWorldScenes()}
function renderWorldScenes(){shell(`<main class="glass rounded-[2rem] p-6"><h2 class="text-3xl font-black">${currentWorld.icon} ${currentWorld.name}</h2><p>${currentWorld.topic} • Difficulty ${state.difficulty}</p><div class="mission rounded-3xl p-5 my-5"><b>Daily Mission:</b> complete a loop, beat a boss, earn coins.</div><div class="grid md:grid-cols-4 gap-4">${scenes.map((s,i)=>`<div class="rounded-[2rem] p-5 bg-white shadow"><div class="text-4xl">${s.icon}</div><h3 class="text-xl font-black min-h-[56px]">${s.name}</h3><button class="sceneBtn btn w-full py-3 mt-3 rounded-2xl bg-slate-900 text-white font-black" data-i="${i}">Start Loop</button></div>`).join('')}</div><button id="backMap" class="btn mt-6 px-5 py-3 rounded-2xl bg-slate-200 font-black">World Map</button></main>`);document.querySelectorAll('.sceneBtn').forEach(b=>b.onclick=()=>openScene(+b.dataset.i));document.getElementById('backMap').onclick=renderWorldMap}
function openScene(i){state.idx=i;state.score=0;state.correct=0;state.lives=3;state.streak=0;state.bossHp=100;state.playerHp=100;renderStory()}
function renderStory(){let s=scenes[state.idx];shell(`<main class="glass rounded-[2rem] p-8 max-w-5xl mx-auto"><h2 class="text-5xl font-black">${s.name}</h2><div class="grid md:grid-cols-4 gap-2 mt-5">${s.loop.map(x=>`<div class="p-3 rounded-xl bg-cyan-50 font-black">${x}</div>`).join('')}</div><button id="startLesson" class="btn mt-6 px-7 py-4 rounded-2xl bg-cyan-500 text-white font-black">Start Learning Loop</button></main>`);document.getElementById('startLesson').onclick=renderWorked}
function renderWorked(){let s=scenes[state.idx];shell(`<main class="glass rounded-[2rem] p-8 max-w-5xl mx-auto"><h2 class="text-4xl font-black">📝 Worked Example</h2><div class="grid gap-4 mt-4">${s.worked.map(x=>`<div class="p-5 rounded-3xl bg-white border shadow"><b>${x[0]}</b><div class="mt-3 p-4 rounded-2xl bg-green-50 text-green-900 font-bold">${x[1]}</div></div>`).join('')}</div><button id="easyBtn" class="btn mt-6 px-7 py-3 rounded-2xl bg-cyan-500 text-white font-black">Easy Mission</button></main>`);document.getElementById('easyBtn').onclick=()=>startMission('easy')}
async function startMission(mode){state.phase=mode;state.q=0;let n=mode==='easy'?3:mode==='mini'?4:mode==='medium'?6:10;state.qs=sample(await getBank(currentWorld.slug,scenes[state.idx].id,currentWorld.topic,state.difficulty),n);renderQuestion()}
function bars(){return state.phase==='boss'?`<div class="grid md:grid-cols-2 gap-3 my-4"><div><b>🐉 Boss HP ${state.bossHp}</b><div class="hp"><div class="hpfill" style="width:${state.bossHp}%"></div></div></div><div><b>🛡️ Player HP ${state.playerHp}</b><div class="hp"><div class="hpfill" style="width:${state.playerHp}%"></div></div></div></div>`:''}
function renderQuestion(msg=''){let s=scenes[state.idx],q=state.qs[state.q];shell(`<main class="glass rounded-[2rem] p-6 max-w-5xl mx-auto"><div class="flex justify-between flex-wrap"><h2 class="text-3xl font-black">${state.phase.toUpperCase()} • ${currentWorld.topic}</h2><div>${badge('Q '+(state.q+1)+'/'+state.qs.length)} ${badge('Score '+state.score,'bg-yellow-100 text-yellow-800')}</div></div>${bars()}<section class="rounded-[2rem] bg-white border p-5 shadow mt-5"><h3 class="text-2xl md:text-3xl font-black">${q.text}</h3><div class="grid gap-3 mt-5">${q.options.map((op,i)=>`<button class="ans target btn p-4 rounded-2xl text-xl md:text-2xl font-black" data-a="${esc(op)}"><span class="enemy">${state.phase==='boss'?'🐉':state.phase==='mini'?'🎣':s.enemy}</span>${op}</button>`).join('')}</div><div class="text-5xl mt-5">🔫 <span class="pet">🦉</span></div></section><div id="fb" class="mt-4">${msg}</div><button id="hintBtn" class="btn mt-4 px-5 py-3 rounded-2xl bg-amber-100 text-amber-800 font-black">TH Hint</button></main>`);document.querySelectorAll('.ans').forEach(b=>b.onclick=()=>answer(unesc(b.dataset.a)));document.getElementById('hintBtn').onclick=()=>document.getElementById('fb').innerHTML=`<div class="p-4 rounded-2xl bg-amber-50 font-bold">🇹🇭 ${q.thai||'อ่านโจทย์อย่างระมัดระวัง'}<br><small>ไม่มีเฉลยคำตอบ</small></div>`}
function answer(a){sfx('laser');let q=state.qs[state.q];sameAnswer(a,q)?correct():wrong()}
function sameAnswer(a,q){return Array.isArray(q.answer)?q.answer.some(x=>same(a,x)):same(a,q.answer)}
function correct(){sfx('correct');state.correct++;state.streak++;state.score+=10;if(state.phase==='boss')state.bossHp=Math.max(0,state.bossHp-15);popText('✅ +10');next(`<div class="p-4 rounded-2xl bg-green-100 font-black">✅ Correct!</div>`,true)}
function wrong(){sfx('wrong');state.streak=0;state.lives--;if(state.phase==='boss')state.playerHp=Math.max(0,state.playerHp-20);state.progress.review.push({world:currentWorld.slug,topic:currentWorld.topic});if(state.lives<=0||state.playerHp<=0)return gameOver();next(`<div class="p-4 rounded-2xl bg-rose-100 font-black">❌ Review later.</div>`,false)}
function next(m,ok){document.getElementById('fb').innerHTML=m;setTimeout(()=>{state.q++;if(state.phase==='boss'&&state.bossHp<=0)return complete();if(state.q>=state.qs.length)return nextLoop();renderQuestion()},ok?500:800)}
function nextLoop(){if(state.phase==='easy')return loopClear('Mini Game',()=>startMission('mini'));if(state.phase==='mini')return loopClear('Medium Mission',()=>startMission('medium'));if(state.phase==='medium'){state.difficulty=difficultyFromAccuracy(state.correct,state.qs.length);return loopClear('Boss Battle',()=>startMission('boss'))}complete()}
function loopClear(label,nextFn){shell(`<main class="glass rounded-[2rem] p-8 max-w-xl mx-auto text-center"><div class="text-7xl">🏅</div><h2 class="text-4xl font-black">Loop Clear!</h2><p>Next: ${label}</p><button id="nextBtn" class="btn mt-7 px-8 py-4 rounded-2xl bg-cyan-500 text-white font-black">Continue</button></main>`);document.getElementById('nextBtn').onclick=nextFn}
function gameOver(){shell(`<main class="glass rounded-[2rem] p-8 max-w-xl mx-auto text-center"><h2 class="text-4xl font-black">Training Review</h2><button id="review" class="btn mt-6 py-4 px-8 rounded-2xl bg-blue-100 text-blue-800 font-black">Review Lesson</button></main>`);document.getElementById('review').onclick=renderWorked}
async function complete(){sfx('victory');let total=state.qs.length,star=calcStars(state.correct,total),coins=star*25+10;state.progress.coins=(state.progress.coins||0)+coins;state.progress.xp+=state.score;sfx('coin');popText('🪙 +'+coins);toast('🏆 Level Complete! Coins +' + coins);save();await saveScore({gameSlug:currentWorld.slug,playerName:state.player,classCode:state.classCode,score:state.score,correct:state.correct,total,stars:star,xp:state.score,coins});shell(`<main class="glass rounded-[2rem] p-8 max-w-2xl mx-auto text-center"><div class="text-8xl">🏆</div><h2 class="text-5xl font-black">Level Complete!</h2><p>Earned 🪙 ${coins}</p><button id="back" class="btn mt-6 py-4 px-8 rounded-2xl bg-cyan-500 text-white font-black">World Scenes</button></main>`);document.getElementById('back').onclick=renderWorldScenes}
function renderShop(){shell(`<main class="glass rounded-[2rem] p-6 max-w-6xl mx-auto"><h2 class="text-4xl font-black">🛒 Adventure Shop</h2><p>Coins 🪙 ${state.progress.coins||0}</p><div class="grid md:grid-cols-4 gap-4 mt-4">${WEAPONS.map(w=>`<div class="card"><div class="text-5xl">${w.icon}</div><h4 class="font-black">${w.name}</h4><p>Cost ${w.cost}</p></div>`).join('')}</div><button id="shopBack" class="btn mt-6 px-6 py-3 rounded-2xl bg-cyan-500 text-white">Back</button></main>`);document.getElementById('shopBack').onclick=renderStart}
async function renderLeaderboard(){let rows=await leaderboard();shell(`<main class="glass rounded-[2rem] p-6 max-w-4xl mx-auto"><h2 class="text-4xl font-black">🏆 Leaderboard</h2><div class="grid gap-2 mt-5">${rows.length?rows.map((r,i)=>`<div class="p-4 rounded-2xl bg-slate-50 flex justify-between"><b>#${i+1} ${r.player_name||'Player'}</b><b>${r.score}</b></div>`).join(''):'<div class="p-4 rounded-2xl bg-slate-50">No scores yet.</div>'}</div><button id="lbBack" class="btn mt-6 px-6 py-3 rounded-2xl bg-cyan-500 text-white">Back</button></main>`);document.getElementById('lbBack').onclick=renderWorldMap}
async function renderStudentDashboard(){let sp=await studentProfile(state.player,state.classCode);shell(`<main class="glass rounded-[2rem] p-6 max-w-5xl mx-auto"><h2 class="text-4xl font-black">Student Dashboard</h2><div class="grid md:grid-cols-4 gap-4 mt-6"><div class="card">XP<br><b class="text-4xl">${sp.wallet?.total_xp||state.progress.xp||0}</b></div><div class="card">Coins<br><b class="text-4xl">${sp.wallet?.total_coins||state.progress.coins||0}</b></div><div class="card">Weapon<br><b class="text-4xl">${weapon().icon}</b></div><div class="card">Pet<br><b class="text-4xl">${pet().icon}</b></div></div><button id="dbBack" class="btn mt-6 px-6 py-3 rounded-2xl bg-cyan-500 text-white">Back</button></main>`);document.getElementById('dbBack').onclick=renderWorldMap}
renderStart();
