export function runQA(worlds){let scenes=0,issues=[];worlds.forEach(w=>(w.scenes||[]).forEach(s=>{scenes++;if(!s.name)issues.push({level:'fail',scope:w.gameName,message:'missing scene name'})}));let fail=issues.length;return{worlds:worlds.length,scenes,questions:worlds.length,issues,fail,warn:0,pass:fail?0:1}}
