import{norm,same}from'./utils.js';
export function runQAQuestions(rows){const issues=[];rows.forEach((q,i)=>{const label=`Q${i+1}`;if(!q.question_en&&!q.text)issues.push({level:'fail',scope:label,message:'Missing question'});const choices=[q.choice_a,q.choice_b,q.choice_c,q.choice_d].filter(x=>x!==undefined);if(choices.length&&choices.length<4)issues.push({level:'fail',scope:label,message:'Less than 4 choices'});const seen=new Set();choices.forEach(c=>{const k=norm(c);if(seen.has(k))issues.push({level:'fail',scope:label,message:`Duplicate choice: ${c}`});seen.add(k)});const answer=q.answer;const correctCount=choices.length?choices.filter(c=>same(c,answer)).length:1;if(choices.length&&correctCount===0)issues.push({level:'fail',scope:label,message:'Answer is not in choices'});if(choices.length&&correctCount>1)issues.push({level:'warn',scope:label,message:'Multiple equivalent correct choices'});if(String(q.question_th||q.thai||'').includes(String(answer)))issues.push({level:'warn',scope:label,message:'Thai hint may reveal answer'});});const fail=issues.filter(x=>x.level==='fail').length,warn=issues.filter(x=>x.level==='warn').length;return{total:rows.length,fail,warn,issues,pass:fail===0}}